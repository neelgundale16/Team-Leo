# Install: pip install -r requirements.txt

# Core API Framework
fastapi
uvicorn[standard]
python-multipart

# Data Validation & Config
pydantic
python-dotenv

# Gemini API (via REST — no SDK needed, using httpx directly)
httpx

# AI / ML — NLI Classifier
transformers
torch
sentence-transformers

# Vector Database
chromadb

# PDF Processing
pypdf

# Dev / Testing
pytest
pytest-asyncio