# ── Project Veracity — Frontend Environment ───────────────────────────────────
# Points the frontend at the running backend
# Do not change this unless you deploy the backend to a different port

NEXT_PUBLIC_BACKEND_URL=http://localhost:8000
